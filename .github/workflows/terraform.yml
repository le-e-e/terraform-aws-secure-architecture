name: Terraform Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  AWS_REGION: ap-northeast-2

jobs:
  terraform:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4   # 리포지토리 코드 가져오기


      - name: AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::556663442070:role/gitaction_rolll
          aws-region: ap-northeast-2


      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3   # Terraform 설치
        with:
          terraform_version: 1.5.7

      - name: Terraform Init
        run: terraform init   # Terraform 초기화

      - name: Terraform Validate
        run: terraform validate   # 코드 문법 검사

      # Checkov 스캔
      - name: Checkov Scan
        uses: bridgecrewio/checkov-action@v4
        continue-on-error: true   # 실패해도 빌드 계속 진행 (결과는 로그에 표시됨)
        with:
          directory: .
          framework: terraform
          soft_fail: true   # Checkov 내부에서도 실패를 경고로 처리

      # Trivy 스캔
      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        continue-on-error: true   # 실패해도 빌드 계속 진행 (결과는 로그에 표시됨)
        with:
          scan-type: "config"
          scan-ref: '.'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0' # 0이면 검출해도 진행, 실무에서는 1로 변경 

      - name: Terraform Plan
        run: terraform plan -out=tfplan   # 변경 사항 계산(plan 생성)

# 실제 배포시 활성화
      # - name: Terraform Apply
      #   if: github.ref == 'refs/heads/main'
      #   run: terraform apply -auto-approve tfplan
